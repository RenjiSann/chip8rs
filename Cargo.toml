[package]
name = "chip8rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[profile.dev]
overflow-checks = false # Necessary, as overflows may occur and are normal

[dependencies]
rand = "0.8.4"
sdl2 = "0.35.1"